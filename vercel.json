{
    "version": 2,
    "buildCommand": "cd VibeCoding/STOCK && npm install && npm run build",
    "outputDirectory": "VibeCoding/STOCK/dist",
    "routes": [
          {
                  "src": "/(.*)",
                  "dest": "/index.html"
          }
    ]
}
